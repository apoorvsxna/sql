-- create a new database.
CREATE DATABASE myDB;

-- delete a database
DROP DATABASE myDB;

-- set as default (can also be done by right clicking on database in workbench)
USE myDB;

-- set as read-only
ALTER DATABASE myDB READ ONLY = 1;